import requests

def news_score(symbol):

    score = 0
    reasons = []

    try:
        # Future: News API integration
        # response = requests.get(...)

        return score, reasons

    except Exception:
        return 0, []
